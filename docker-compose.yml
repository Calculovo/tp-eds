services:
  db:
    image: postgres:16
    container_name: restaurank-db
    environment:
      POSTGRES_USER: restaurank
      POSTGRES_PASSWORD: restaurank
      POSTGRES_DB: restaurank
    ports:
      - "5432:5432"
    volumes:
      - restaurank_pgdata:/var/lib/postgresql/data
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql

volumes:
  restaurank_pgdata:
